/*
 * StoryLink Messages
 *
 * This contains all the text for the StoryLink component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.StoryLink';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StoryLink component!',
  },
});
